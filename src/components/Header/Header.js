import React from 'react';
import './Header.css'

const Header = () => {
    return (
        <header className="App-header">
            <h1>Notes App</h1>
        </header>
    );
}

export default Header;
